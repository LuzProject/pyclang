[tool.poetry]
name = "pyclang"
version = "0.0.1"
description = "Interface clang through Python"
authors = ["ja1dan <ajaidan0@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.7"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
